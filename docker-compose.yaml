version: '2'

services:
  dmall-registry-server:
      image: 10.204.21.177:5000/dmall/shipping-service:latest
      mem_limit: 512m
      networks:
          - jenkins_dmall-network
      ports:
          - "8081:8081"

networks:
  jenkins_dmall-network:
    external: true